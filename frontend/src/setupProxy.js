// This file is used to configure the development server
// It's automatically picked up by Create React App

const { createProxyMiddleware } = require('http-proxy-middleware');

module.exports = function(app) {
  // Add any proxy configurations here if needed
  
  // This is just a placeholder to ensure the file is loaded
  console.log('Setup proxy loaded');
}; 
